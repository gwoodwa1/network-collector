# Copy outside the repository, replace the placeholders, then run:
# chmod 600 /secure/path/credentials.yaml
default:
  username: automation
  password: replace-me
profiles:
  datacenter:
    username: eos-automation
    password: replace-me
  edge:
    username: junos-automation
    password: replace-me
